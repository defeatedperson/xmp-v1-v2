ssh 登录 docker 仓库账号

```bash
docker login -u defeatedperson
```

构建并推送 PHP 7.2 镜像

```bash
docker build -t defeatedperson/php:v7.2.0 ./php72
docker push defeatedperson/php:v7.2.0
```

构建并推送 PHP 7.3 镜像

```bash
docker build -t defeatedperson/php:v7.3.0 ./php73
docker push defeatedperson/php:v7.3.0
```

构建并推送 PHP 7.4 镜像

```bash
docker build -t defeatedperson/php:v7.4.0 ./php74
docker push defeatedperson/php:v7.4.0
```

构建并推送 PHP 7.4-opcache 镜像

```bash
docker build -t defeatedperson/php:v7.4.1 ./php74
docker push defeatedperson/php:v7.4.1
```

构建并推送 PHP 8.5 镜像

```bash
docker build -t defeatedperson/php:v8.5.0 ./php85
docker push defeatedperson/php:v8.5.0
```

构建并推送 PHP 8.1 镜像

```bash
docker build -t defeatedperson/php:v8.1.0 ./php81
docker push defeatedperson/php:v8.1.0
```

构建并推送 PHP 8.4 镜像

```bash
docker build -t defeatedperson/php:v8.4.0 ..
docker push defeatedperson/php:v8.4.0
```

